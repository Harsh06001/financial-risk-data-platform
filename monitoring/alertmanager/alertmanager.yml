global:
  resolve_timeout: 5m

route:
  receiver: local-webhook
  group_by: [alertname, severity]
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 4h

receivers:
  - name: local-webhook
    webhook_configs:
      - url: http://alert-webhook:8088/alerts
        send_resolved: true
