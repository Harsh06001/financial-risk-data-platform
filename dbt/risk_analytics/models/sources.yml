version: 2

sources:
  - name: risk_analytics
    database: "{{ env_var('GCP_PROJECT_ID', 'risk-data-platform-npg-2026') }}"
    schema: risk_analytics
    tables:
      - name: daily_transaction_summary
      - name: customer_risk_features
      - name: merchant_risk_features
      - name: segment_risk_summary
      - name: high_risk_transactions
      - name: processed_transactions
      - name: streaming_transaction_events
      - name: streaming_transaction_events_quarantine
      - name: streaming_pipeline_observations
